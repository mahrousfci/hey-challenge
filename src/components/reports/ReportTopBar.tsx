import styled from 'styled-components';
import theme from '../../theme';

const ReportTopBar = styled.div`
  display: flex;
  justify-content: space-between;
  padding: ${theme.spacing(4)};
`;

export default ReportTopBar;
