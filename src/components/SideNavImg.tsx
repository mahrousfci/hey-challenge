import styled from 'styled-components';
import theme from '../theme';

const SideNavImg = styled.img`
  width: 26px;
  height: 218px;
  margin-top: ${theme.spacing(2)};
`;

export default SideNavImg;
